node_modules/
.next/
.env
.env*.local
out/
*.tsbuildinfo
next-env.d.ts
.vercel
.DS_Store

# NOTE: /data/*.json and /public/search-index.json ARE committed so the Vercel
# build never depends on the data-generation step running first. They are still
# regenerated by `npm run build` / `npm run dev` (predev), so redeploys stay fresh.
