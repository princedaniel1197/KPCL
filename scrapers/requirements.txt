# Sentinel scraper suite — Python 3.11+
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
tenacity>=8.2

# PDF extraction (CAG / KERC / annual reports)
pdfplumber>=0.11

# OCR for scanned PDFs (KERC tariff orders, some CAG reports).
# Also needs the Tesseract binary installed — see RUNBOOK.md.
pytesseract>=0.3.10
pdf2image>=1.17          # rasterize scanned PDF pages for OCR
Pillow>=10.0

# Headless browser for JS-rendered portals (KERC listings, e-Proc, Parivesh).
# After install run: python -m playwright install chromium
playwright>=1.44
