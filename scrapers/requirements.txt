# Sentinel scraper suite — Python 3.11+
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pdfplumber>=0.11
tenacity>=8.2
# Only needed if a source requires a JS render (Parivesh sometimes does).
# Install browsers with: python -m playwright install chromium
playwright>=1.44
